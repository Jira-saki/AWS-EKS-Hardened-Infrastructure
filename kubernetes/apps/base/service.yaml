apiVersion: v1
kind: Service
metadata:
  name: secure-api-svc
  labels:
    app: secure-api
spec:
  selector:
    app: secure-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
      protocol: TCP
